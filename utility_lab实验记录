本实验为第一个实验，主要是熟悉一下xv6以及简单的系统函数调用
sleep --- 调用sleep函数即可，睡眠时间单位为100ms
pingpong --- 创建一对管道，一个管道用于从父进程传递到子进程，一个管道用于从子进程传递反馈到父进程
primes --- 主要参考实验指南上提供的那张图，采用递归，依次向下传递，每一个进程输出一个质数，注意记得及时关闭文件描述符，不然资源不足以输出2->35之间的质数，（我试了一下好像可以输出到37）
find --- 考察对文件树的搜索，搜索对应文件夹下的文件，以及递归搜索子文件夹下的文件
xargs --- 该函数的功能是通过包装从而使得某些函数接受标准输入作为函数的参数，因为有些函数只能在命令行直接输入参数
（具体见user文件下相应文件名）
